apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calendar-bot.fullname" . }}-test-connection"
  labels:
    {{- include "calendar-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calendar-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
