apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "github-matrix-project-bot.fullname" . }}-test-connection"
  labels:
    {{- include "github-matrix-project-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "github-matrix-project-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
